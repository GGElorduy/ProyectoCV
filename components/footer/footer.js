import './footer.css'

import { personalInfo } from '../../assets/aboutMe'
/* export const printFooter = () => {
  const footer = document.querySelector('footer')
  const ulContact = document.createElement('ul')
  footer.appendChild(ulContact)

  for (const key in personalInfo.contacts) {
    const li = document.createElement('li')
    const link = document.createElement('a')
    const icon = document.createElement('img')
    icon.src = personalInfo.contacts[key].icon
    link.href = personalInfo.contacts[key].adress

    ulContact.appendChild(li)
    li.appendChild(link)
    link.appendChild(icon)
  }
} */
